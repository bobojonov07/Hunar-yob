
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Маълумоти асосии корбар ва ҳолати верификатсия",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "role": { "type": "string", "enum": ["Usto", "Client"] },
        "phone": { "type": "string" },
        "region": { "type": "string" },
        "balance": { "type": "number" },
        "identificationStatus": { "type": "string", "enum": ["None", "Pending", "Verified", "Rejected", "Blocked"] }
      }
    },
    "VerificationRequest": {
      "title": "VerificationRequest",
      "description": "Дархости муфассали верификатсия бо ҳуҷҷатҳо ва чек",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "userPhone": { "type": "string" },
        "photos": { "type": "array", "items": { "type": "string" } },
        "receipt": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Verified", "Rejected", "Blocked"] },
        "submittedAt": { "type": "string", "format": "date-time" },
        "errorReason": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["email", "google"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Профилҳои асосии корбарон"
    },
    "/verification_requests/{userId}": {
      "schema": "VerificationRequest",
      "description": "Навбати баррасии ҳуҷҷатҳо ва чекҳо (KYC Queue)"
    },
    "/listings/{listingId}": {
      "schema": "Listing",
      "description": "Эълонҳои устоҳо"
    }
  }
}
